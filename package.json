{
  "name": "nestjs-swr-cache",
  "version": "1.0.0",
  "description": "NestJS module implementing the serverside Stale-While-Revalidate caching pattern using Redis.",
  "publisher": "devyeon",
  "repository": "ksi05503/nestjs-swr-cache",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && tsc",
    "prepublish": "npm run build"
  },
  "author": "devyeon",
  "license": "MIT",
  "dependencies": {
    "@nestjs/common": "^10.1.3",
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.4.9",
    "typescript": "^5.1.6"
  },
  "peerDependencies": {
    "ioredis": "^5.3.2"
  }
}
